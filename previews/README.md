# previews/

Preview images for gallery entries, shown in the in-app browser. One image per entry.

Naming convention: `previews/<entry-id>.png` or `previews/<entry-id>.jpg`, matching the registry entry ID. Keep images at or under 512 KB (CI enforces this) and make them an honest screenshot of the content actually running in Beatform.
